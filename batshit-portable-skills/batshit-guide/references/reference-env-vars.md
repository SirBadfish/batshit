# Environment Variables

This page maps the environment variables users and operators are most likely to touch. It is not a replacement for `.env.example`, `.env.docker.example`, or service-specific docs; it is the practical "what is this for?" layer.

## Env file map

| File | Used by | Notes |
| --- | --- | --- |
| `~/Library/Application Support/Batshit/config/runtime.env` | Mac app runtime | Generated by Runtime Doctor/supervisor with local secrets and runtime defaults. Do not publish it. |
| `~/Library/Application Support/Batshit/config/api-key-encryption.key` | Mac app API-key encryption | Durable recovery copy of the Mac app encryption key. Batshit creates it with owner-only permissions. Do not edit, delete, publish, or paste it into chat. |
| `.env.example` | Advanced source-checkout setup source material | Copy to `.env` only when running Batshit directly from source on the host for repair or development. |
| `.env.docker.example` | Docker setup | Copy to `.env.docker` before Docker setup. `./start-docker.sh` prepares missing stable local values. |
| `batshit-app/.env.example` | SvelteKit app source material | Mostly useful for app-level source-checkout repair/development configuration. |
| `batshit-server/server/.env.example` | batshit-server source material | Used when running batshit-server outside Docker and outside the Mac app supervisor. |

Docker users must run Compose with `--env-file .env.docker` whenever they rely on `.env.docker` for port or public URL overrides. Docker Compose's `env_file` passes values into containers, while `--env-file` also feeds Compose variable interpolation.

## Required stable secrets

| Variable | Required for | What it does | Notes |
| --- | --- | --- | --- |
| `BATSHIT_TOKEN` | Real Mac app, advanced source-checkout, and Docker instances | Internal service token shared by Batshit, batshit-server, and trusted helper calls. | Keep stable. Current official n8n Native Tools use scoped per-message tokens, but rotating `BATSHIT_TOKEN` still requires updating any deliberate service-token caller. Runtime launchers create it when missing, and Settings -> API Keys shows it as a runtime-managed/copyable value instead of saving a second encrypted row. |
| `ENCRYPTION_KEY` | Saving API keys/secrets | Encrypts saved provider keys and custom secrets. | Keep stable. If it changes, saved encrypted secrets may stop decrypting. |

Use long random values. A local command such as `openssl rand -hex 32` is a reasonable way to generate each value. Do not publish real values in docs, screenshots, logs, issues, or templates.

## Mac app and advanced source-checkout core variables

These apply when Batshit runs through `Batshit.app` or directly from source on the host. Normal Mac users should let Runtime Doctor generate and manage the Mac app runtime config. Source-checkout setup is repair/development material, not a normal public install path.

| Variable | Default example | Purpose |
| --- | --- | --- |
| `NODE_ENV` | Mac app: `production`; source checkout: `development` | Node runtime environment. |
| `BATSHIT_FRONTEND_HOST` | source checkout: `127.0.0.1` | Vite dev-server bind host for source-checkout Batshit. Keep this IPv4 loopback unless `N8N_BATSHIT_FRONTEND_URL` is also changed, because local n8n native-tool callbacks use `127.0.0.1`. |
| `BATSHIT_FRONTEND_PORT` | Mac app: `5620`; source checkout: `5621` | Browser-facing Batshit app port. |
| `BATSHIT_SERVER_PORT` | Mac app: `5600`; source checkout: `5610` | batshit-server port used by the Mac app supervisor and source-checkout repair launchers. |
| `BATSHIT_FRONTEND_URL` | Mac app: `http://127.0.0.1:5620`; source checkout: `http://localhost:5621` | Browser-facing Batshit base URL used by local runtime helpers. |
| `ORIGIN` | Mac app: `http://127.0.0.1:5620`; source checkout: `http://localhost:5621` | Public app origin used by SvelteKit/session handling. |
| `PUBLIC_BASE_URL` | Mac app: `http://127.0.0.1:5620`; source checkout: `http://localhost:5621` | Public Batshit base URL returned to browser-facing paths. |
| `BATSHIT_SERVER_URL` | Mac app: `http://127.0.0.1:5600`; source checkout: `http://localhost:5610` | App/server URL for batshit-server origin. |
| `BATSHIT_SERVER_API_URL` | Mac app: `http://127.0.0.1:5600/api/v1`; source checkout: `http://localhost:5610/api/v1` | batshit-server API root. |
| `PUBLIC_BATSHIT_SERVER_URL` | Mac app: `http://127.0.0.1:5600`; source checkout: `http://localhost:5610` | Browser-facing batshit-server URL saved/returned to users. |
| `PUBLIC_BATSHIT_SERVER_API_URL` | Mac app: `http://127.0.0.1:5600/api/v1`; source checkout: `http://localhost:5610/api/v1` | Browser-facing batshit-server API root. |
| `BATSHIT_ARTIFACT_COMPLETE_URL` | Mac app: `http://127.0.0.1:5620/api/artifacts/complete`; source checkout: `http://localhost:5621/api/artifacts/complete` | Server-to-app artifact completion callback. |
| `BATSHIT_APP_VERSION` | `0.1.0-alpha.1` | Current installed Batshit version shown by the in-app update check. Normal users should not change this. |
| `BATSHIT_APP_CHANNEL` | `alpha` | Release channel label shown by the in-app update check. |
| `BATSHIT_UPDATE_CHECK_DISABLED` | `0` | Set to `1` to disable in-app update checks for offline/private environments. |
| `BATSHIT_UPDATE_FEED_URL` | blank | Optional custom latest-release feed. Blank uses Batshit's official GitHub release feed. |
| `BATSHIT_UPDATE_LATEST_VERSION` | blank | Optional local/test override for the latest available version. Leave blank for normal installs. |
| `BATSHIT_UPDATE_RELEASE_URL` / `BATSHIT_UPDATE_DOWNLOAD_URL` | blank | Optional local/test links used with `BATSHIT_UPDATE_LATEST_VERSION`. |
| `N8N_BATSHIT_FRONTEND_URL` | Mac app: `http://127.0.0.1:5620`; Docker: the app/container-reachable Batshit URL | Server-to-server Batshit app URL that n8n workflows call for native-tool dispatch and callbacks. |
| `BODY_SIZE_LIMIT` | Mac app: `1G`; source checkout: unset/SvelteKit default unless you set it | SvelteKit incoming request body limit for ordinary app requests and large imports. Backup export and restore use dedicated disk streams instead. |
| `REDIS_URL` | Mac app: `redis://127.0.0.1:5639/0`; source checkout: `redis://localhost:6379/0` | Redis connection string. |
| `REDIS_HOST` | Mac app: `127.0.0.1`; source checkout: `localhost` | Redis host when not using `REDIS_URL`. |
| `REDIS_PORT` | Mac app: `5639`; source checkout: `6379` | Redis port when not using `REDIS_URL`. |
| `BATSHIT_MAC_REDIS_PORT` | `5639` | Override for the Mac-app-owned Redis port before Runtime Doctor creates the runtime config. |
| `DOCKER_MCP_GATEWAY_URL` / `DOCKER_MCP_GATEWAY_PORT` | Blank unless configured; when blank, the code falls back to `http://localhost:8080` (host) / `http://host.docker.internal:8080` (container) | Optional Docker MCP Gateway endpoint. The active runtime launcher uses these values when it owns gateway startup. |
| `MCP_GATEWAY_AUTH_TOKEN` | generated by the active runtime | Bearer token for Docker MCP Gateway HTTP/streaming access. Batshit generates this before starting the gateway so users do not have to find Docker's hidden generated token. |
| `DOCKER_MCP_PROFILE` | `default` unless changed | Docker MCP Toolkit profile ID. Mac app/source-checkout profile changes apply after restarting Batshit; Docker installs change it in `.env.docker` and recreate/restart containers. Changing this value clears the cached Docker gateway tool list so old profile tools are not shown as current. |
| `BATSHIT_USER_ID` | `user_default` | Single-user instance identity. Most users should not change it after setup. |

Provider keys can be supplied through env, but Settings -> API Keys is the friendlier setup path for normal users.

The Mac app also uses macOS folders instead of repo-local data paths:

| Location | Purpose |
| --- | --- |
| `~/Library/Application Support/Batshit` | Durable app data and generated runtime config |
| `~/Library/Logs/Batshit` | Runtime logs |
| `~/Library/Caches/Batshit` | Cache and scratch data |

## Docker core variables

These live in `.env.docker`.

| Variable | Default example | Purpose |
| --- | --- | --- |
| `BATSHIT_DOCKER_BIND_HOST` | `127.0.0.1` | Host address Docker publishes ports on. The default keeps Batshit reachable only from the computer running Docker. `0.0.0.0` opts into LAN exposure — only on a trusted network. |
| `BATSHIT_DOCKER_APP_PORT` | `5620` | Host/browser port for the Docker Batshit app. |
| `BATSHIT_DOCKER_SERVER_PORT` | `5600` | Host/browser port for Docker batshit-server. |
| `BATSHIT_DOCKER_N8N_PORT` | `5678` | Host/browser port for the optional Docker n8n profile. |
| `ORIGIN` | `http://localhost:5620` | Public app origin used by SvelteKit/session handling. |
| `PUBLIC_BASE_URL` | `http://localhost:5620` | Browser-facing Batshit base URL. |
| `PUBLIC_BATSHIT_SERVER_URL` | `http://localhost:5600` | Browser-facing batshit-server URL. |
| `PUBLIC_BATSHIT_SERVER_API_URL` | `http://localhost:5600/api/v1` | Browser-facing batshit-server API URL. |
| `BATSHIT_ARTIFACT_COMPLETE_URL` | blank | Leave blank for Docker's internal default `http://app:3000/api/artifacts/complete`. |
| `BATSHIT_SESSION_COOKIE_SECURE` | blank | Leave blank for automatic local HTTP vs HTTPS behavior. |
| `BATSHIT_SESSION_COOKIE_NAME` | `batshit_session_docker` | Lets Docker stay logged in beside another local Batshit instance. |
| `BODY_SIZE_LIMIT` | `1G` | SvelteKit incoming request body limit for ordinary app requests and trusted local imports. It is not a backup export or restore limit. |
| `BATSHIT_APP_VERSION` | `0.1.0-alpha.1` | Current installed Batshit version shown by the in-app update check. |
| `BATSHIT_APP_CHANNEL` | `alpha` | Release channel label shown by the in-app update check. |
| `BATSHIT_UPDATE_CHECK_DISABLED` | `0` | Set to `1` to disable in-app update checks. |
| `BATSHIT_UPDATE_FEED_URL` | blank | Optional custom latest-release feed. Blank uses Batshit's official GitHub release feed. |
| `BATSHIT_UPDATE_LATEST_VERSION` | blank | Optional local/test override for the latest available version. Leave blank for normal installs. |
| `BATSHIT_UPDATE_RELEASE_URL` / `BATSHIT_UPDATE_DOWNLOAD_URL` | blank | Optional local/test links used with `BATSHIT_UPDATE_LATEST_VERSION`. |

Docker core containers also set internal values such as `BATSHIT_CONTAINERIZED=1`, `BATSHIT_SERVER_URL=http://batshit-server:5600`, and app `PORT=3000` from `compose.yaml`. Users normally should not override those internal service values.

## Docker workspace and CLI variables

| Variable | Default example | Purpose |
| --- | --- | --- |
| `BATSHIT_WORKSPACE_MOUNT` | blank, filled by `./start-docker.sh` | Host folder mounted into Docker at `/workspace`. |
| `BATSHIT_CODEX_WORKDIR` | `/workspace` | Working directory for Docker CLI/Codex agents. |
| `BATSHIT_AGENT_RUNTIME_ROOT` | `/home/batshit-cli/.batshit` | Docker app path for managed CLI installs, Claude auth state, and managed Claude profiles. |
| `BATSHIT_MANAGED_INSTALLS_ROOT` | `/home/batshit-cli/.batshit/installs` | Docker app path for one-click managed CLI/runtime installs. |
| `BATSHIT_CLAUDE_RUN_AS_USER` | `batshit-cli` | Non-root Docker user used for Claude Code child processes and Claude auth/status commands. |
| `BATSHIT_CLAUDE_RUN_AS_UID` / `BATSHIT_CLAUDE_RUN_AS_GID` | `10001` / `10001` | UID/GID used when Batshit spawns Claude Code inside Docker. This keeps Claude Bypass Permissions out of root. |
| `BATSHIT_CLAUDE_RUN_AS_HOME` | `/home/batshit-cli` | HOME used for Docker Claude Code auth and runtime files. |
| `BATSHIT_CODEX_PROVIDER_ENABLED` | `true` | Enables the Docker Codex provider path when the CLI is authenticated. |

For Docker installs, the Codex/Claude Code CLI is not bundled in the image. Install it with the one-click card in Agent Settings (saved in the persistent `batshit_agent_runtime` volume), then log each CLI in inside the app container before its `CLI` agents can run. Batshit shows Docker-aware commands such as:

```sh
docker compose --env-file .env.docker exec app /home/batshit-cli/.batshit/installs/cli/bin/codex login --device-auth
docker compose --env-file .env.docker exec --user batshit-cli --env HOME=/home/batshit-cli --env USER=batshit-cli --env LOGNAME=batshit-cli --env SHELL=/bin/sh app /home/batshit-cli/.batshit/installs/cli/bin/claude auth login
```

When the stack was started with optional Docker n8n profile, the command may include `--profile n8n` before `exec`. Claude commands include the non-root Docker user flags so Claude Code status, login, and Bypass Permissions use the same HOME.

On Windows, Claude Code may print `Paste code here if prompted >` after the browser login. Paste the browser auth code at that prompt, then press Enter. The pasted code may not appear in the terminal.

Optional overrides: `BATSHIT_CODEX_CLI_PATH` / `CODEX_CLI_PATH` and `BATSHIT_CLAUDE_CLI_PATH` / `CLAUDE_CLI_PATH` point Batshit at a specific CLI executable and win over the managed install and PATH lookup.

Advanced tuning: `BATSHIT_CODEX_CONTEXT_GUARD_THRESHOLD` and `BATSHIT_CLAUDE_CONTEXT_GUARD_THRESHOLD` adjust when Batshit gracefully stops a long managed CLI run to relay it before the model's context window fills (default `0.8`, valid from `0.5` inclusive to `1` exclusive). Either variable accepts `off`, `false`, `disabled`, `none`, or `0` to disable its guard. Invalid values fail the run before the CLI starts instead of silently using a different threshold. `BATSHIT_CODEX_TRANSPORT=exec` reverts normal managed Codex chat runs to the one-shot exec transport, which has no live token feed and therefore no proactive guard; Batshit's hidden Codex maintenance summarizer remains on the guarded app-server lane.

## Docker Sandbox and runtime add-on operator

`./start-docker.sh` prepares these values automatically for normal Docker setup.

| Variable | Default example | Purpose |
| --- | --- | --- |
| `BATSHIT_NATIVE_DOCKER_SANDBOX_DRIVER` | `operator` | Tells Dockerized Batshit to use Batshit's host operator for Docker Sandbox. |
| `BATSHIT_DOCKER_SANDBOX_OPERATOR_URL` | filled by launcher | App-container URL for the host Sandbox operator, usually `http://host.docker.internal:5629`. |
| `BATSHIT_DOCKER_SANDBOX_OPERATOR_TOKEN` | generated by launcher | Bearer token for Sandbox operator calls. |
| `BATSHIT_DOCKER_SANDBOX_OPERATOR_TIMEOUT_MS` | `30000` | Timeout for Sandbox operator requests. |
| `BATSHIT_SANDBOX_CONTAINER_WORKSPACE_ROOT` | `/workspace` | Container path mapped to the host workspace. |
| `BATSHIT_SANDBOX_HOST_WORKSPACE_ROOT` | filled by launcher | Real host path mapped to `/workspace`. |
| `BATSHIT_RUNTIME_ADDON_OPERATOR_URL` | filled by launcher | App-container URL for approved add-on start/stop controls. |
| `BATSHIT_RUNTIME_ADDON_OPERATOR_TOKEN` | generated by launcher | Bearer token for approved add-on controls. |
| `BATSHIT_RUNTIME_ADDON_OPERATOR_TIMEOUT_MS` | `180000` | Longer timeout for add-on starts/stops. |

Raw Docker Compose can run without these values, but Docker Sandbox and Batshit-managed add-on start/stop buttons will show unavailable/manual setup states until the operator is configured.

## Docker MCP Gateway variables

Docker MCP Gateway is optional and host-side.

| Variable | Default example | Purpose |
| --- | --- | --- |
| `DOCKER_MCP_GATEWAY_PORT` | blank | Host port for the optional Docker MCP Gateway. |
| `DOCKER_MCP_GATEWAY_URL` | blank | Explicit gateway URL. Host gateways usually become `host.docker.internal` from the app container. |
| `DOCKER_MCP_PROFILE` | blank or `default` | Docker MCP profile to expose. |
| `MCP_GATEWAY_AUTH_TOKEN` | blank until configured/generated | Bearer token Batshit and n8n must use for gateway calls. |

When `DOCKER_MCP_GATEWAY_PORT` or `DOCKER_MCP_GATEWAY_URL` is set in `.env.docker`, `./start-docker.sh` starts the host gateway with the configured token/profile. Core Batshit does not require Docker MCP Gateway.

## n8n variables

| Variable | Default example | Purpose |
| --- | --- | --- |
| `N8N_API_URL` | native: `http://localhost:5678`; Docker host-managed: `http://host.docker.internal:5678`; optional Docker n8n profile: `http://n8n:5678` | n8n API URL Batshit should call server-side. |
| `N8N_API_KEY` | blank | Optional n8n API key for Batshit-side n8n API/MCP integrations. For native `n8n` Primary hydration and Stop-button cancellation, scoped keys need execution list/read/stop plus workflow list/read. This is not Docker MCP Gateway auth. |
| `N8N_UI_EMAIL` | blank | Optional n8n UI login for parameter discovery/automation where supported. |
| `N8N_UI_PASSWORD` | blank | Optional n8n UI login password. |
| `BATSHIT_N8N_CALLBACK_BASE_URL` | Docker default `http://app:3000` | Callback base optional Docker n8n profile can use to reach Batshit. |
| `N8N_BASE_IMAGE` | `ghcr.io/n8n-io/n8n:2.22.1` | Optional Docker n8n profile image. |
| `N8N_RUNNERS_IMAGE` | `ghcr.io/n8n-io/runners:2.22.1` | Optional Docker n8n profile task-runner image. |
| `N8N_RUNNERS_AUTH_TOKEN` | blank, defaults to `BATSHIT_TOKEN` when blank | Internal n8n runner token. |
| `N8N_EDITOR_BASE_URL` | `http://localhost:5678` | Browser/editor URL for optional Docker n8n profile. |
| `WEBHOOK_URL` | `http://localhost:5678/` | Browser-facing webhook base for optional Docker n8n profile. |
| `N8N_WEBHOOK_URL` | `http://localhost:5678/webhook` | Browser-facing webhook URL for optional Docker n8n profile. |
| `GENERIC_TIMEZONE` | `America/New_York` | n8n timezone. |
| `TZ` | `America/New_York` | Container timezone. |

When using `./start-docker.sh --profile n8n`, the launcher sets `N8N_API_URL=http://n8n:5678` if `.env.docker` still has a default host-managed n8n URL. If you use direct Compose with `--profile n8n`, set that value yourself. Keep the editor/webhook values browser-facing.

Provider credentials for n8n workflows belong in n8n. Batshit does not copy provider API keys into n8n.

## Provider and registry variables

These can be env fallbacks, but Settings -> API Keys is preferred for normal use.

| Variable | Purpose |
| --- | --- |
| `OPENAI_API_KEY` | OpenAI provider key. |
| `ANTHROPIC_API_KEY` | Anthropic provider key. |
| `GOOGLE_GENERATIVE_AI_API_KEY` | Google Gemini provider key. |
| `MISTRAL_API_KEY` | Mistral provider key. |
| `GROQ_API_KEY` | Groq provider key. |
| `MOONSHOT_API_KEY` / `MOONSHOT_API_BASE_URL` | Moonshot AI/Kimi provider key and optional OpenAI-compatible base URL override. |
| `MINIMAX_API_KEY` / `MINIMAX_API_BASE_URL` | MiniMax key and optional OpenAI-compatible base URL override. The same key also powers MiniMax TTS. |
| `MIMO_API_KEY` / `MIMO_API_BASE_URL` | MiMo key and optional OpenAI-compatible base URL override. The same key also powers MiMo TTS. |
| `ALIBABA_CLOUD_API_KEY` / `DASHSCOPE_API_KEY` | Alibaba Cloud Model Studio / DashScope key for Qwen-compatible model presets and Alibaba TTS. |
| `ALIBABA_CLOUD_API_BASE_URL` / `DASHSCOPE_API_BASE_URL` | Optional Alibaba OpenAI-compatible chat base URL override. |
| `ALIBABA_CLOUD_TTS_BASE_URL` / `DASHSCOPE_TTS_BASE_URL` | Optional Alibaba TTS base URL override. |
| `STEPFUN_API_KEY` / `STEP_API_KEY` / `STEPFUN_API_BASE_URL` | StepFun key and optional OpenAI-compatible base URL override. The same key also powers StepFun TTS. |
| `OPENROUTER_API_KEY` | OpenRouter provider key. |
| `AI_GATEWAY_API_KEY` | Vercel AI Gateway key. |
| `DEEPSEEK_API_KEY` | DeepSeek provider key in app env examples. |
| `ZAI_API_KEY` / `ZAI_CODING_API_KEY` | Z.ai provider keys in app env examples. |
| `DEEPINFRA_API_KEY`, `TOGETHER_API_KEY`, `FIREWORKS_API_KEY`, `BASETEN_API_KEY`, `CEREBRAS_API_KEY` | Optional OpenAI-compatible provider keys in app env examples. |
| `FISH_AUDIO_API_KEY` | Optional Fish Audio key for direct realtime TTS. Fish also needs a voice/reference ID in Voice Settings or the request. |
| `INWORLD_API_KEY`, `CARTESIA_API_KEY`, `ASYNC_API_KEY` | Optional batch TTS provider keys. |
| `AZURE_SPEECH_KEY`, `AZURE_SPEECH_REGION` | Optional Microsoft Azure Speech REST TTS credentials. Azure viseme/lip-sync events need a future SDK bridge, not only these REST values. |
| `BATSHIT_MODEL_CATALOG_URL` | Hosted model catalog URL, default `https://api.batshit.ai/registry/catalog.json`. |
| `ARTIFICIAL_ANALYSIS_API_KEY` | Optional model catalog enrichment. |
| `KV_REST_API_URL`, `KV_REST_API_TOKEN`, `KV_REST_API_READ_ONLY_TOKEN` | Registry/model catalog maintenance, not normal first-run setup. |

## Optional Docker add-on variables

### Cloudflared

| Variable | Default example | Purpose |
| --- | --- | --- |
| `CLOUDFLARED_RELEASE_TAG` | `2026.3.0` | Sidecar version. |
| `CLOUDFLARED_LINUX_AMD64_SHA256` / `CLOUDFLARED_LINUX_ARM64_SHA256` | pinned hashes | Build-time integrity checks. |
| `BATSHIT_CLOUDFLARED_TARGET_URL` | `http://batshit-server:5600` | Internal target for Clip tunnel URLs in Docker. |
| `BATSHIT_CLOUDFLARED_METRICS` | `0.0.0.0:20241` | Sidecar metrics bind. |
| `BATSHIT_CLOUDFLARED_HEARTBEAT_SECONDS` | `15` | Sidecar heartbeat interval. |

### FBX-to-VRMA

| Variable | Default example | Purpose |
| --- | --- | --- |
| `FBX2GLTF_VERSION` | `v0.9.7` | Worker dependency version. |
| `FBX2GLTF_LINUX_X64_SHA256` | pinned hash | Build-time integrity check. |
| `BATSHIT_FBX2VRMA_WORKER_URL` | `http://fbx2vrma-worker:8079` | Internal worker URL. |
| `BATSHIT_FBX2VRMA_MAX_BYTES` | `524288000` | Max upload size for conversion. |
| `BATSHIT_FBX2VRMA_CONVERT_TIMEOUT_MS` | `120000` | Conversion timeout. |

### NVIDIA Audio2Face bridge

These variables configure Batshit's optional bridge. NVIDIA Audio2Face-3D NIM v2.0 is a separate GPU runtime with separate NVIDIA terms; Batshit does not download or bundle it.

| Variable | Default example | Purpose |
| --- | --- | --- |
| `BATSHIT_AUDIO2FACE_BRIDGE_URL` | `http://audio2face-bridge:8068` | Internal app-to-bridge URL. |
| `BATSHIT_AUDIO2FACE_BRIDGE_TOKEN` | generated by launcher | Distinct private bridge token. Raw Compose users must set one. |
| `BATSHIT_AUDIO2FACE_NIM_ENDPOINT` | `host.docker.internal:52000` | NVIDIA NIM v2.0 gRPC endpoint reachable from the bridge. |
| `BATSHIT_AUDIO2FACE_NIM_TLS` | `0` | Enables TLS for the bridge-to-NIM gRPC connection. |
| `BATSHIT_AUDIO2FACE_NIM_ROOT_CERT` | blank | Optional root certificate path for TLS. |
| `BATSHIT_AUDIO2FACE_NIM_CLIENT_CERT` / `BATSHIT_AUDIO2FACE_NIM_CLIENT_KEY` | blank | Optional mTLS client certificate and key paths. |
| `BATSHIT_AUDIO2FACE_NIM_TIMEOUT_SECONDS` | `180` | Completed-utterance inference timeout. |
| `BATSHIT_AUDIO2FACE_CACHE_MAX_BYTES` | `2147483648` | Completed-result cache size limit. |
| `BATSHIT_AUDIO2FACE_CACHE_MAX_ENTRIES` | `2000` | Completed-result cache entry limit. |
| `BATSHIT_AUDIO2FACE_TONGUE_ENABLED` | `0` | Requests NVIDIA's optional 16-channel tongue output. |

### Agent Browser

| Variable | Default example | Purpose |
| --- | --- | --- |
| `AGENT_BROWSER_VERSION` | `0.24.1` | Pinned Agent Browser CLI version for Docker sidecar. |
| `BATSHIT_AGENT_BROWSER_SIDECAR_URL` | `http://agent-browser:8091` | Internal sidecar URL. |
| `BATSHIT_AGENT_BROWSER_SIDECAR_TOKEN` | generated by launcher | Sidecar token. |
| `BATSHIT_AGENT_BROWSER_TMP_DIR` | `/runtime/agent-browser/tmp` | Shared screenshot/temp directory. |

### LiveKit

LiveKit is a voice runtime, not a TTS/STT engine.

| Variable | Default example | Purpose |
| --- | --- | --- |
| `LIVEKIT_SERVER_IMAGE` | `livekit/livekit-server:v1.13.5` | Docker LiveKit image tested with this Batshit release. |
| `LIVEKIT_SERVER_INSTALL_ROOT` | `~/.batshit/installs/livekit-server` | Native managed LiveKit Server install root override. |
| `BATSHIT_DOCKER_LIVEKIT_PORT` | `7880` | Browser/API/WebSocket port. |
| `BATSHIT_DOCKER_LIVEKIT_RTC_TCP_PORT` | `7881` | WebRTC TCP port. |
| `BATSHIT_DOCKER_LIVEKIT_RTC_UDP_PORT` | `7882` | WebRTC UDP port. |
| `LIVEKIT_URL` | `ws://localhost:7880` | Browser-facing LiveKit URL. |
| `LIVEKIT_INTERNAL_URL` | `ws://host.docker.internal:7880` | App-container dispatch URL. |
| `LIVEKIT_AGENT_LIVEKIT_URL` | `ws://livekit:7880` | Agent sidecar to LiveKit server URL. |
| `LIVEKIT_AGENT_INSTALL_ROOT` | `~/.batshit/installs/livekit-sidecar` | Native managed Batshit LiveKit sidecar install root override. |
| `LIVEKIT_AGENT_SOURCE_ROOT` | bundled sidecar package | Advanced native sidecar source-package override for reinstall/repair. Packaged Mac apps set this to the bundled runtime copy. |
| `LIVEKIT_AGENT_HEALTH_URL` | `http://livekit-agent:7899/worker` | Worker health URL. |
| `LIVEKIT_API_KEY` | `batshit-local` | Local API key for native or optional Docker LiveKit. |
| `LIVEKIT_API_SECRET` | placeholder/generated | Local API secret. Keep stable. |
| `LIVEKIT_VOICE_AGENT_NAME` | `batshit-livekit-agent` | Voice agent worker name. |
| `LIVEKIT_AGENT_BATSHIT_BASE_URL` | `http://app:3000` | Worker callback base URL. |
| `LIVEKIT_AGENT_BATSHIT_TOKEN` | blank, defaults to `BATSHIT_TOKEN` in Compose | Worker callback token. |

### ComfyUI validation

| Variable | Default example | Purpose |
| --- | --- | --- |
| `BATSHIT_COMFYUI_VALIDATION_URL` | `http://comfyui:8188` | Internal validation sidecar URL. |
| `BATSHIT_DOCKER_COMFYUI_VALIDATION_PORT` | `8188` | Host/browser port for the validation sidecar. |

The ComfyUI validation sidecar is for runtime checks, not a full GPU ComfyUI distribution.

## batshit-server variables

These matter when batshit-server runs outside the Docker Compose core.

| Variable | Default example | Purpose |
| --- | --- | --- |
| `BATSHIT_SERVER_HOST` | `127.0.0.1` | Bind host for batshit-server. |
| `PORT` | `5600` | batshit-server port. |
| `ENABLE_HTTPS` | `false` | Enables HTTPS in standalone server contexts. |
| `HTTPS_ONLY` | `false` | Forces HTTPS-only behavior when configured. |
| `BATSHIT_SERVER_LOG_DIR` | Mac app: `~/Library/Logs/Batshit`; source-checkout default: current working directory | Directory for batshit-server `error.log` and `combined.log`. The Mac app supervisor sets this so logs do not write inside the signed app bundle. |
| `UPLOADS_DIR` | `./uploads` | Upload storage root. |
| `BATSHIT_BACKUP_STAGING_DIR` | Runtime-managed private data folder | Shared private restore staging root used by the app and batshit-server. Mac and Docker configure it automatically; custom source deployments must point both processes at the same directory. |
| `BATSHIT_REDIS_REQUIRED` | `false` native example, `true` in Docker | Whether server health/startup requires Redis. |

## Logging and internal dev variables

| Variable | Default example | Purpose |
| --- | --- | --- |
| `BATSHIT_LOG_LEVEL` | `info` | Batshit server/app logging level. |
| `BATSHIT_HTTP_LOGS` | `true` | HTTP request logging toggle. |
| `VITE_LOG_LEVEL` | `info` | Browser/app debug log level. |
| `VITE_DEBUG_MODE` | `true` | Enables debug traces in local contexts. |
| `REDIS_LOG_LEVEL` | `info` | Redis helper logging level. |
| `VITE_SUPPRESS_WARNINGS` | `true` in app example | Hides some browser warnings unless set false. |
| `BATSHIT_ENABLE_INTERNAL_DEV_PANEL` | `0` | Internal development panel. Public installs should leave disabled. |
| `BATSHIT_INTERNAL_DEV_PANEL_SECRET` | blank | Required when internal dev panel is intentionally enabled. |

## Values you should usually leave alone

- Docker internal service names such as `http://app:3000`, `http://batshit-server:5600`, and `http://n8n:5678`.
- `BATSHIT_CONTAINERIZED`, which Compose sets for Docker.
- Internal compatibility flags that are not listed in current env examples.
- Packaged image versions unless following a documented update.
- Checksum variables unless updating the matching binary/image source.
- Internal dev panel variables.
- Tokens printed in logs or copied from someone else's machine.
